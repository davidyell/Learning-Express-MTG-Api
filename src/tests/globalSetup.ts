/* eslint-disable no-console */

const setup = async () => {
  console.log('> Creating testing database...');
  // TODO: Run a Prisma migration file, which includes just the card data required
  // TODO: Remove the github actions step of building the database
};
export default setup;
